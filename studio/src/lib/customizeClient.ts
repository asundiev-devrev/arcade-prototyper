// customizeClient.ts now only houses KEPT functions for the instant-edit + props path.
// The detach/Customize apparatus (serializeTargetToJsx, markJsxRoot, newCustomizeToken,
// postCustomize, postCustomizeUndo, buildCustomizePayload) was removed in Task 3 —
// props-first editing (Task 2) replaced it.
