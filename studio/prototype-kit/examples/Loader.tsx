import React from "react";
import { Loader } from "../arcade-components";

export default (
  <div className="flex items-center gap-3">
    <Loader size="sm" />
    <Loader size="md" />
    <Loader size="lg" />
  </div>
);
